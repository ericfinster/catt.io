coh id {C : Cat} (x :: C)
  : x => x

coh comp {C : Cat}
    {x :: C}
    {y :: C} (f :: x => y)
    {z :: C} (g :: y => z)
  : x => z

let prune_l {C : Cat}
    {x :: C}
    {y :: C} (f :: x => y)
  : [ x => y ] = comp (id x) f
