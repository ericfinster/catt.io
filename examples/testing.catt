#
#  testing.catt - seeing what we can do
#

let J {C : Cat}
    (x :: Arr C)
    (y :: Arr C) (f :: x => y)
                 (g :: x => y) (a :: f => g)
    (z :: Arr C) (h :: y => z) 
  : [ base z => lid z ]
  = 


