#
#  demo.catt - new syntax demo and whatnot
#

let idf (A : U) (a : A) (b : A) (c : A) : A = a
let dlb (A : U) (f : (a : A) -> A) (a : A) : A = f (f a)

# coh id (C : Cat)
#     (x :: C)
#   : x -> x

# coh comp (C : Cat)
#     (x :: C)
#     (y :: C) (f :: x -> y)
#     (z :: C) (g :: y -> z)
#   : x -> z


